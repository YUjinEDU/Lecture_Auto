---
phase: 01.1-mvp-demo
verified: 2026-03-24T00:00:00Z
status: human_needed
score: 6/7 must-haves verified
human_verification:
  - test: "Run: python run.py --pptx <test.pptx> --minutes 5, answer y at each [y/n] prompt"
    expected: "All 6 stages complete, output/lecture_{job_id}.mp4 is playable with slide images and Korean audio, output/summary_{job_id}.json contains all 6 stage timings"
    why_human: "E2E pipeline requires live GPU with Qwen3-VL + Qwen3-TTS loaded, real PPTX input, and ffmpeg on PATH — cannot verify programmatically without GPU hardware"
---

# Phase 01.1: MVP Demo Verification Report

**Phase Goal:** `python run.py --pptx file.pptx` — PPTX in, lecture script + audio out — all major components integrated and functional enough for a professor to evaluate.
**Verified:** 2026-03-24
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `python run.py --pptx file.pptx` 실행 시 6단계 파이프라인이 순차 실행된다 | ✓ VERIFIED | run.py (502 lines): all 6 stages wired with parse_pptx, render_slides, generate_visual_notes, generate_scripts, synthesize_audio, assemble_video |
| 2 | 매 단계 완료 후 [y/n] 프롬프트로 사용자가 중간 결과를 확인/편집할 수 있다 | ✓ VERIFIED | `confirm_step()` called after each of stages 1-5; `Stage 1/6` through `Stage 6/6` literals present |
| 3 | Qwen3-VL이 슬라이드별 시각 노트 JSON을 생성한다 | ✓ VERIFIED | vlm.py: `from vllm import LLM`, `VlmNote` schema with all 6 fields, `generate_visual_notes()` saves `vlm_note_{N:03d}.json`, text-grounded prompting implemented |
| 4 | Claude가 강의 스크립트를 한국어로 생성하며 앞뒤 슬라이드 문맥이 반영된다 | ✓ VERIFIED | script_gen.py: `asyncio.create_subprocess_exec("claude", "-p", ...)`, prev/next slide context in `build_script_prompt()`, Korean professor persona |
| 5 | Qwen3-TTS가 스크립트를 음성으로 합성하며 voice clone을 지원한다 | ✓ VERIFIED | tts.py: `Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice`, `voice_ref_path` param in `synthesize_slide()`, `sf.read(voice_ref_path)` for clone |
| 6 | ffmpeg가 슬라이드 PNG + 오디오 WAV를 단일 MP4로 조립한다 | ✓ VERIFIED | video.py: `ffmpeg -loop 1 ... -shortest`, concat demuxer, `libx264`, per-slide `clip_{N:03d}.mp4` cleaned up after concat |
| 7 | output/summary_{job_id}.json에 전체 파이프라인 요약이 저장된다 | ✓ VERIFIED | run.py: `write_summary()` writes `stage_timings`, `total_seconds`, `output_mp4`, `work_dir` to `output/summary_{job_id}.json` |

**Score:** 7/7 automated truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lecture_auto/pipeline/vlm.py` | VLM visual notes generation | ✓ VERIFIED | Substantive: VlmNote schema, load_vlm, build_vlm_prompt, generate_visual_notes. Wired: imported in run.py line 225, in __init__.py |
| `lecture_auto/pipeline/script_gen.py` | Claude-based script generation | ✓ VERIFIED | Substantive: SlideScript schema, call_claude, build_script_prompt, generate_scripts. Wired: imported in run.py line 226, in __init__.py |
| `lecture_auto/pipeline/tts.py` | TTS audio synthesis | ✓ VERIFIED | Substantive: load_tts, synthesize_slide, synthesize_audio, voice_ref_path, 24000 Hz. Wired: imported in run.py line 227, in __init__.py |
| `lecture_auto/pipeline/video.py` | ffmpeg video assembly | ✓ VERIFIED | Substantive: create_slide_clip, concat_clips, assemble_video, -loop 1, -shortest, libx264. Wired: imported in run.py line 228, in __init__.py |
| `run.py` | Interactive CLI orchestrator | ✓ VERIFIED | 502 lines (>150 min). All argparse flags, confirm_step, 6-stage pipeline, write_summary |
| `lecture_auto/pipeline/__init__.py` | Pipeline package exports | ✓ VERIFIED | Re-exports all 8 pipeline symbols |
| `tests/test_vlm.py` | VLM unit tests | ✓ VERIFIED | 8 tests pass (part of 83 total) |
| `tests/test_script_gen.py` | Script gen unit tests | ✓ VERIFIED | 8 tests pass |
| `tests/test_tts.py` | TTS unit tests | ✓ VERIFIED | 10 tests pass |
| `tests/test_video.py` | Video unit tests | ✓ VERIFIED | 11 tests pass |
| `tests/test_run.py` | Orchestrator unit tests | ✓ VERIFIED | 45 tests pass; 83 total across all 5 test files |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `vlm.py` | `vllm.LLM` | `from vllm import LLM, SamplingParams` | ✓ WIRED | Line 17 of vlm.py |
| `script_gen.py` | `claude -p` | `asyncio.create_subprocess_exec("claude", "-p", ...)` | ✓ WIRED | `call_claude()` function, line 185 |
| `tts.py` | Qwen3-TTS | `Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice` default, soundfile for output | ✓ WIRED | `_DEFAULT_MODEL`, `soundfile` import at line 14 |
| `video.py` | ffmpeg | `subprocess.run(["ffmpeg", ...])` | ✓ WIRED | `create_slide_clip()` and `concat_clips()` both call subprocess.run with ffmpeg |
| `run.py` | `parser` | `from lecture_auto.pipeline.parser import parse_pptx` | ✓ WIRED | Lazy import line 223 |
| `run.py` | `renderer` | `from lecture_auto.pipeline.renderer import render_slides` | ✓ WIRED | Lazy import line 224 |
| `run.py` | `vlm` | `from lecture_auto.pipeline.vlm import load_vlm, generate_visual_notes` | ✓ WIRED | Lazy import line 225 |
| `run.py` | `script_gen` | `from lecture_auto.pipeline.script_gen import generate_scripts` | ✓ WIRED | Lazy import line 226 |
| `run.py` | `tts` | `from lecture_auto.pipeline.tts import load_tts, synthesize_audio` | ✓ WIRED | Lazy import line 227 |
| `run.py` | `video` | `from lecture_auto.pipeline.video import assemble_video` | ✓ WIRED | Lazy import line 228 |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| VLM-01 | 01.1-01 | All slides → Qwen3-VL → visual_summary, key_elements, layout_relations, teaching_points, possible_confusions | ✓ SATISFIED | VlmNote schema has all 5 fields; generate_visual_notes iterates all slides |
| VLM-02 | 01.1-01 | Parsed text provided alongside image (text-grounded prompting) | ✓ SATISFIED | `"Parsed text from this slide:\n{parsed_text_block}"` in build_vlm_prompt |
| VLM-03 | 01.1-01 | VLM output validated against JSON schema | ✓ SATISFIED | `VlmNote.model_validate_json()` called per slide; retry on parse failure |
| SCRIPT-01 | 01.1-01 | Slide structure + VLM notes → Claude → lecture script | ✓ SATISFIED | build_script_prompt includes shapes text + vlm_note dict; call_claude subprocess |
| SCRIPT-02 | 01.1-01 | Prev/next slide context reflected in script generation | ✓ SATISFIED | `prev_slide`, `next_slide`, `prev_script` params in build_script_prompt |
| SCRIPT-03 | 01.1-01 | Target time controls per-slide script length | ✓ SATISFIED | `target_seconds = (manifest.target_minutes * 60) / manifest.slide_count` |
| SCRIPT-04 | 01.1-01 | Output includes slide_id, target_seconds, script, keywords, transition_to_next | ✓ SATISFIED | SlideScript schema has all required fields (slide_index, slide_number, target_seconds, script, keywords, transition_to_next) |
| TTS-01 | 01.1-02 | Script → Qwen3-TTS → per-slide WAV audio | ✓ SATISFIED | synthesize_audio() iterates scripts, calls synthesize_slide(), writes audio_{N:03d}.wav |
| TTS-02 | 01.1-02 | Voice clone from reference WAV (3-sec audio) | ✓ SATISFIED | voice_ref_path param throughout; sf.read(voice_ref_path) passed as voice_ref kwarg |
| TTS-04 | 01.1-02 | All slide audio merged into single lecture audio | ✓ SATISFIED | assemble_video() concatenates per-slide clips (PNG+WAV) into single MP4 via ffmpeg concat; audio is embedded in the MP4 |

**Note on TTS-04:** The requirement says "전체 슬라이드 음성을 하나의 강의 음성으로 병합" (merge all slide audio into one lecture audio). The implementation merges via ffmpeg video assembly (WAV embedded in MP4) rather than producing a standalone merged WAV file. This satisfies the spirit of TTS-04 for the MVP demo context (the professor evaluates the MP4). A standalone merged WAV may be needed in Phase 4.

---

## Anti-Patterns Found

No anti-patterns detected across vlm.py, script_gen.py, tts.py, video.py, or run.py.

- No TODO/FIXME/PLACEHOLDER comments
- No stub return values (return {}, return [], return None without logic)
- No hardcoded empty data flowing to output
- No placeholder implementations

---

## Human Verification Required

### 1. End-to-End Pipeline Execution

**Test:** Prepare a PPTX file (3-5 slides, Korean content preferred). Run:
```
python run.py --pptx <path_to_test.pptx> --minutes 5
```
Answer `y` at each `[y/n]` prompt.

**Expected:**
- 6 stages complete without error
- Intermediate files exist in `/data/work/{job_id}/`: `parsed/manifest.json`, `rendered/slide_001.png`, `vlm/vlm_note_001.json`, `scripts/script_001.json`, `audio/audio_001.wav`
- `output/lecture_{job_id}.mp4` is playable with slide images and Korean lecture audio
- `output/summary_{job_id}.json` contains `stage_timings` with all 6 keys

**Why human:** Requires live GPU with Qwen3-VL-8B-Instruct and Qwen3-TTS-12Hz-1.7B-CustomVoice loaded, authenticated `claude` CLI, ffmpeg on PATH, and a real PPTX file. All components are correctly wired in code but hardware execution cannot be verified programmatically.

---

## Gaps Summary

No automated gaps found. All 7 success criteria from ROADMAP.md are satisfied by the implementation. All 10 required requirement IDs (VLM-01, VLM-02, VLM-03, SCRIPT-01, SCRIPT-02, SCRIPT-03, SCRIPT-04, TTS-01, TTS-02, TTS-04) are fully implemented and evidenced in the codebase.

The one item requiring human confirmation is the full E2E hardware execution, which was noted as a blocking checkpoint (Task 2 of Plan 03) in the SUMMARY. The codebase is complete and ready for that test.

---

_Verified: 2026-03-24_
_Verifier: Claude (gsd-verifier)_
