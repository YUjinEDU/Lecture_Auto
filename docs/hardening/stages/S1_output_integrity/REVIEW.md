# S1 REVIEW (감독: Opus, 2026-09-23)

**판정: ✅ 병합 가능 (사용자 승인 대기)** — 후속 커밋 `5372c8c`로 요청 사항 반영, 281 passed / 4 failed 재확인, 재사용 테스트 2건은 수정 전 코드에서 실패 확인(작업자), master+S1+S4-a 시험 병합 충돌 없음

## 확인한 것
| 항목 | 결과 |
|---|---|
| import 경로 | worktree 내부 확인(작업자 출력) |
| 전체 pytest 재실행(감독) | 277 passed / 4 failed — 남은 4건은 기준선 실패, 신규 실패 0 |
| **기준 코드에서 새 테스트 실패 여부** | `983d1e8` 임시 worktree에 새 `test_tts.py`/`test_video.py`만 넣어 실행 → 핵심 5건 실패(목록 병합 2, strict 3). 즉 테스트가 실제로 결함을 잡음. `test_batch_generate_lectures.py`는 신규 함수 import라 기준에서 당연히 실패 |
| S1-a | 목록 분기에서 glob 없음. 디렉터리 호출자 3곳 동작 불변 |
| S1-b | strict에서 "경로는 있으나 파일 없음"도 잡음 — 배치는 슬라이드마다 경로를 무조건 만들기 때문에 필요한 보강(스펙보다 나음) |
| S1-c | 기존 WAV 존재 시 `.cand.wav`에만 씀, 기존 hash 미기록 확인. `--slides` 밖 슬라이드 유지 분기는 기존대로 |
| S1-d/e | 키 계산 단일 함수로 통일 → 합성·검사 키 불일치 위험 제거 |
| 분기점 | `983d1e8..ad05c65` 사이 해당 파일 변경 없음 → master 병합 충돌 없음 |
| test_video 기존 테스트 수정 | 구현(단일 패스 ffmpeg)과 어긋난 mock을 현실에 맞춤 — 타당 |

## 요청한 후속 수정
1. 후보 재사용: `--slides` 강제가 아니고 `.cand.wav.json`의 `cache_key`가 같으면 재합성 생략.
   이유: S1-d로 기존 hash가 전부 무효 → 매 전체 실행마다 77개 슬라이드를 후보로 재합성(GPU 수 시간).
2. 테스트 3종(키 일치 스킵 ok/fail, 키 불일치 합성, 강제 합성)
3. `failed_slides` 로그 문구 정정

## 운영 메모
- S1 병합 후 실제 배치를 돌리면 모든 슬라이드가 후보로 합성되고 결과는 `_DRAFT.mp4`. S2(승인 목록) 전에는 실행하지 않는다(D-06).
